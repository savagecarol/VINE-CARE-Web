version: '3.8'

services:
  react-app:
    build:
      context: .
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL:-http://localhost/api}
    container_name: react-app
    ports:
      - "8080:80"
    restart: always